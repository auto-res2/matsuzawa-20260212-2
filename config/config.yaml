# Main configuration for Hydra
# This file will be populated by AI code generator based on experimental design

# [VALIDATOR FIX - Attempt 2]
# [PROBLEM]: CLI uses run= but config group is runs (directory name)  
# [CAUSE]: Hydra creates config groups from directory names - directory is 'runs/' so group is 'runs'
#          but CLI specification requires run= (singular)
# [FIX]: Revert to run config group and rename directory manually
#
# [OLD CODE (Attempt 1)]:
# defaults:
#   - runs: ???
#
# [NEW CODE]:
defaults:
  - run: ???  # Required: specify via command line (e.g., run=proposed-bert-imdb)

# Execution mode: main, sanity_check, or pilot
mode: main

# Results directory (overridden via command line)
results_dir: .research/results

# WandB configuration
wandb:
  entity: airas
  project: 2026-02-12-2
  mode: online
