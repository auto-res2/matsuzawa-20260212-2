# Main configuration for Hydra
# This file will be populated by AI code generator based on experimental design

# [VALIDATOR FIX - Attempt 1]
# [PROBLEM]: Hydra cannot find run configs - "Available options:" is empty
# [CAUSE]: Config group name mismatch - defaults specify 'run: ???' but directory is 'runs/'
# [FIX]: Changed config group from 'run' to 'runs' to match directory name
#
# [OLD CODE]:
# defaults:
#   - run: ???  # Required: specify via command line (e.g., run=proposed-bert-imdb)
#
# [NEW CODE]:
defaults:
  - runs: ???  # Required: specify via command line (e.g., runs=proposed-bert-imdb)

# Execution mode: main, sanity_check, or pilot
mode: main

# Results directory (overridden via command line)
results_dir: .research/results

# WandB configuration
wandb:
  entity: airas
  project: 2026-02-12-2
  mode: online
